class Solution {
public:
    int removeDuplicates(vector<int>& nums) {

        sort(nums.begin(),nums.end());

        int uniq = unique(nums.begin(), nums.end()) - nums.begin();
        
        return uniq; // return number of unique elements
    }
};
